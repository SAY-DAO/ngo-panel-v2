import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer = () => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography>Social Workers Gateway </Typography>
  </Box>
);

export default Footer;
